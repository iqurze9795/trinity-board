import { Helmet } from "react-helmet";

const PriceTitle = () => {
  return (
    <Helmet defer={false}>
      <title>
        Trinity board | Best DEFI traking tool.
      </title>
    </Helmet>
  );
};

export default PriceTitle;
